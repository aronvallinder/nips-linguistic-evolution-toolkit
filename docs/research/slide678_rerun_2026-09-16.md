# Slide 678 transplant rerun — September 16, 2026

Status: complete. All 35 final-state runs passed request-profile, context,
replicate-identity, and ten-round completion validation. No error snapshots
remain in the output. The completion receipt hashes every included final.

## Question and scope

Does the seven-cell historical seed ladder persist under the explicit September
Sonnet request profile? Ivar authorized the faithful rerun after reviewing the
June 22–23 rationale for myth-only newcomer context. Linguistic analysis should
precede any later content-removal treatment; no deletion or strategy-summary
condition is included here.

Seven cells × five replicates × eight agents × ten game rounds = 35 runs and
2,800 planned calls. Cells: no seed, Wikipedia filler, early Sonnet myth, late
high-cooperation Sonnet myth, late low-cooperation myth, late Gemini myth, and
late GPT myth. Existing `data/phase3/seed_manifest.json` texts are reused, one
donor per replicate, shared by the eight agents in that run. Different donor
texts and stochastic run variation are therefore both represented in cell SDs.

## Preserved apparatus

- Game only; no myth generation. The identical seed is supplied every round.
- Baseline: system + current game prompt. Seeded: system + myth-writing request
  + inherited myth + current game prompt. No earlier game messages or separate
  own/co-player history block. Round number and visible cumulative earnings remain.
- Eight anonymous agents, balanced randomized dyadic roles; ten rounds;
  $5 endowment; 3× multiplier; uniform negative-$5 communication noise on both
  transfers, uninformed. This is not the later negative-$1 comparison design.
- Fresh unseeded pairing/noise draws, as in the original runner; not paired
  common random numbers or exact historical trajectories.

Preparation verified all 35 historical final files against the selected seed
and freshly rendered system/myth prefix. Full mocked simulations checked all
seven cells, 80 decisions each, including final-state validation and skip behavior.

## Explicit differences and limits

Native Anthropic `claude-sonnet-4-5-20250929`, thinking enabled with 8,192-token
budget, maximum output 64,000 tokens, temperature omitted. Historical finals do
not fully identify their provider request policy, so this is an apparatus rerun
under a pinned profile, not a claim of identical historical inference settings.
The current strict role/JSON response validation and same-prompt retry-once
boundary also apply; corrective retry prompts are disabled.

No automatic whole-run resampling. Complete-round checkpoints are resumable;
partial-round error snapshots are not proof of completion. Any continuation
after a partial failed round must disclose discarded attempts. The frozen plan
hashes implementation sources, old finals, donors, templates and settings.

## Execution and cost

Runner: `scripts/rerun_slide678.py`; tests: `tests/test_slide678_rerun.py`.
Output root: `data/json/noise_experiments/slide678_rerun_20260916/`.
The `plan.json` is the resolved, frozen input; per-cell `repNN.json` files are
full-state finals, with adjacent checkpoint/log files.

Estimated standard-rate cost is about $40 from the old input-token totals and
September pilot output usage; preflight allowance is $60 for all 35. This is
an estimate, not a bill or a hard output-token spending cap.

```sh
python scripts/rerun_slide678.py prepare
python scripts/rerun_slide678.py run --workers 2 --limit 2
python scripts/rerun_slide678.py audit
python scripts/rerun_slide678.py run --workers 4
```

The second run command only selects missing finals; historical outputs are
never overwritten. Inspect the initial baseline and late-Sonnet requests before
releasing the other 33. Do not edit frozen implementation files during execution.

The supervisor began with four workers after the initial two-cell process. At
Ivar's request it was changed to 20 workers. Ivar then explicitly chose a clean
restart of every incomplete run: partial checkpoints, error snapshots, and
incomplete-run logs were deleted; the two fully completed and validated smoke
runs were preserved. It validates full finals before continuing in
batches no larger than the selected worker count, rechecking actual token costs and contexts between
batches. A failed batch stops for inspection instead of resampling. A projected
total over $100 stops for approval. Live status is in `supervisor_status.json`;
only after all 35 validate does it write `completion_receipt.json` with final
hashes, token costs and per-cell means/SDs. These files are under the output root.
For a concise live view, `scripts/watch_slide678_progress.py` rewrites
`progress.log` in the same output root as a current-state list: exactly one line
per completed or active run, including condition, replicate, rounds, decisions,
and recorded successful-call cost.

## Result

The final joint resources after ten rounds were:

| Injected context | Mean joint resources (±SD) | n |
|---|---:|---:|
| No inherited text | 424.6 (±2.07) | 5 |
| Unrelated Wikipedia filler | 440.2 (±7.09) | 5 |
| Early Sonnet myth | 540.0 (±37.47) | 5 |
| Late high-cooperation Sonnet myth | 600.0 (±0.00) | 5 |
| Late low-cooperation myth | 384.0 (±87.12) | 5 |
| Late high-cooperation Gemini myth | 573.8 (±48.98) | 5 |
| Late high-cooperation GPT myth | 455.4 (±32.07) | 5 |

The qualitative historical ladder reappeared under the pinned profile: the late
Sonnet myths reached the cooperation ceiling in every replicate, early Sonnet
and late Gemini myths increased resources relative to the baseline, the filler
was near baseline, and the low-cooperation myths reduced the mean with substantial
variation. Late GPT myths were also near baseline. With only five donor/run
replicates per cell, these are descriptive results; donor content and stochastic
run variation are not separately estimated. This does not establish that the
narrative form itself caused the effect.

The 35 retained finals record $27.502824 at standard token rates. This is not an
invoice total: incomplete attempts deliberately discarded during the concurrency
restart and any transport-level retries are absent. See `completion_receipt.json`
in the output root for hashes, per-run usage, raw values, and this cost boundary.
